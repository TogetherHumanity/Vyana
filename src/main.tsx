import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => {
  return <h1 style={{ color: 'white', textAlign: 'center', marginTop: '2rem' }}>Welcome to VYANA</h1>;
};

ReactDOM.createRoot(document.getElementById('root')!).render(<App />);